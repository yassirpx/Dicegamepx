function Btn({roll,ssave,hhhh} ){
    return(<div>
        <button onClick={roll}>Roll</button>
        <button onClick={ssave}>Save</button>
        <button onClick={hhhh}>New Game</button>
         </div>
   );

}
export default Btn;